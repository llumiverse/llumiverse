import { assert } from "chai";
import { readDataFile } from "./utils.js";

// const content = readDataFile('template.jst');
// const expectedOut = readDataFile('template-out.txt');

// const input = { questions: ["Question 1", "Question 2"] };

// function compareIgnoreSpaces(text1: string, text2: string) {
//     const lines1 = text1.trim().split("\n");
//     const lines2 = text2.trim().split("\n");
//     if (lines1.length !== lines2.length) {
//         return false;
//     }
//     for (let i = 0, l = lines1.length; i < l; i++) {
//         if (lines1[i].trim() !== lines2[i].trim()) return false;
//     }
//     return true;
// }

// // chai expect example
// describe('JS text Templates', () => {
//     it('rendering works', () => {
//         const out = renderJsTemplate(content, ['questions'], input);
//         assert(compareIgnoreSpaces(out, expectedOut), "Output does not match")
//     });
// })

